<%= form_with(model: sonos_system) do |form| %>
  <% if sonos_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sonos_system.errors.count, "error") %> prohibited this sonos_system from being saved:</h2>

      <ul>
        <% sonos_system.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :ip %>
    <%= form.text_field :ip %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
